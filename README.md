# roblox_1
A fresh Rojo project configured for a clean baseplate scene. Use `rojo build` to produce a place file or `rojo serve` while editing.

## Commands
- `rojo build -o build.rbxlx`
- `rojo serve`
